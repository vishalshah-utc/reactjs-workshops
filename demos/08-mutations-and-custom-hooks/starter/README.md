# ShopScope — Demo 8 starter

```bash
npm install
npm run dev
```

**Your instructions are in [the demo guide](../README.md).**

## Where you are starting from

**Demo 7, finished** — server-side search/filters/pagination, categories on
their own request, a detail drawer. Add and delete are still browser-only.

New stub: `src/hooks/useApi.ts`.

## What you build

Search for `TODO(lab-` — eight markers.

| Marker | File |
|---|---|
| `lab-1.1` | `src/api/services/products.ts` — `POST` / `PATCH` / `DELETE` |
| `lab-2.1` | `src/components/ProductForm.tsx` — async submit, edit mode |
| `lab-2.2`, `3.1`, `4.2` | `src/App.tsx` — save, delete, `useApi` refactor |
| `lab-2.3` | `src/components/ProductCard.tsx` — edit button |
| `lab-4.1` | `src/hooks/useApi.ts` |
| `lab-4.3` | `src/components/ProductDetail.tsx` |

**DummyJSON simulates writes** — requests and responses are real, persistence
isn't. Refresh to reset.

## ⚠️ Don't re-click the starter link

Bookmark the `stackblitz.com/edit/…` URL once it loads.

## Finished version

[`../../09-routing-with-react-router/starter`](../../09-routing-with-react-router/starter).

## Commands

```bash
npm run dev · npm run typecheck · npm run lint · npm run build
```

Node 22.22+.
