/**
 * The cart's rules as a PURE function over CartLine[] — no React, no store.
 * There is no cart UI yet; Demo 13 gives this function a home inside a store.
 * A reducer is just (state, action) => newState. useReducer is one place it can live.
 */
// TODO(lab-1.3): CartAction union (add / setQty / remove / clear) and cartLinesReducer(lines, action): CartLine[] — plus lineCount(lines) and subtotal(lines)
export {};
