import type { AxiosInstance } from 'axios';

/** Does nothing yet. Lab 4.2: tag every request with an X-Request-Id and log its lifecycle. */
// TODO(lab-4.2): request interceptor (id + start time in config.metadata) and response interceptors (timing) — and RETURN config/response
export function installLoggingInterceptor(_instance: AxiosInstance) {}
