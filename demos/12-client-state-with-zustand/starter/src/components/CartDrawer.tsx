/**
 * The cart drawer. Rendered once, in the layout; opened from the header;
 * filled from the product pages — all through the cart store, no props.
 */
// TODO(lab-2.3): an Offcanvas over useCartStore — lines with quantity buttons and remove, subtotal; useShallow for the object of actions
// TODO(lab-4.4): a fetcher.Form that posts to /account/checkout (signed in) or a "Sign in to check out" link
export function CartDrawer() {
  return null;
}
