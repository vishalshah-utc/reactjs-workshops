// TODO(lab-2.2): two variant maps with class-variance-authority — `badge` (tone: ok | low | out | neutral; size: sm | md | lg;
// pill: boolean) and `button` (tone: primary | light | outlineLight | ghost | link; size: sm | md | lg; block: boolean) — both
// over Bootstrap's own classes, plus `BadgeVariants` / `ButtonVariants` derived with VariantProps<typeof …>.
export {};
