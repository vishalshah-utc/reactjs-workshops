import type { PriceTagProps } from '../PriceTag';

// TODO(lab-1.1): Way 1 — plain CSS. Import './pricetag.css' (a side-effect import), render the same markup as PriceTag
// with BEM class names (price, price__final, price__was, price__off, price--sm / price--lg).
export function PriceTagPlain(_props: PriceTagProps) {
  return null;
}
