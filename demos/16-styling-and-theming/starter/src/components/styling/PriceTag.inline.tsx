import type { PriceTagProps } from '../PriceTag';

// TODO(lab-1.3): Way 3 — inline `style`. CSSProperties objects: camelCase keys, bare numbers are pixels, strings for any
// other unit. Then try to add :hover — you will need useState and two mouse handlers, which is the lesson.
export function PriceTagInline(_props: PriceTagProps) {
  return null;
}
