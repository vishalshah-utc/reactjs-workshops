import type { PriceTagProps } from '../PriceTag';

// TODO(lab-1.4): Way 4 — Tailwind v4. Import '../../tailwind.css' HERE (not in main.tsx), then prefixed utilities:
// tw:flex tw:gap-2 tw:font-semibold tw:text-xl tw:text-emerald-600 tw:dark:text-emerald-400 tw:hover:… — full class
// names only; Tailwind scans source text, so `tw:text-${x}` generates nothing.
export function PriceTagTailwind(_props: PriceTagProps) {
  return null;
}
