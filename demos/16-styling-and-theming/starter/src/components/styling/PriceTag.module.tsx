import type { PriceTagProps } from '../PriceTag';

// TODO(lab-1.2): Way 2 — CSS Modules. `import styles from './PriceTag.module.css'` returns an OBJECT of hashed class names;
// render with className={styles.price} etc. Conditional size classes by hand: [a, cond && b].filter(Boolean).join(' ').
export function PriceTagModule(_props: PriceTagProps) {
  return null;
}
