import type { PriceTagProps } from '../PriceTag';

// TODO(lab-1.5): Way 5 — Bootstrap utilities: a frozen copy of today's src/components/PriceTag.tsx, for the comparison.
export function PriceTagBootstrap(_props: PriceTagProps) {
  return null;
}
