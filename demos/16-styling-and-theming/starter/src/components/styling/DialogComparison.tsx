// TODO(lab-5.3): two buttons that open the SAME ConfirmDialogProps through two implementations — React Bootstrap's Modal
// (styled) and ConfirmDialogHeadless (native <dialog>) — and report which one confirmed or cancelled.
export function DialogComparison() {
  return null;
}
