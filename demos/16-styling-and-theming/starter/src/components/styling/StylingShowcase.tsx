// TODO(lab-1.5): render the five PriceTag variants side by side with ONE sample product (price 64, 25% off) — a Card per way,
// with the file name and what to look for in DevTools → Elements. Rendered by AboutPage. Lab 5.3 adds <DialogComparison /> below it.
export function StylingShowcase() {
  return null;
}
