import type { ConfirmDialogProps } from './ConfirmDialog';

// TODO(lab-5.1): a HEADLESS ConfirmDialog on the native <dialog>: a ref, an effect that calls showModal()/close() when `show`
// changes, onCancel (Escape) and onClose events, a backdrop click, and styles from ConfirmDialog.module.css. About 40 lines.
export function ConfirmDialogHeadless(_props: ConfirmDialogProps) {
  return null;
}
