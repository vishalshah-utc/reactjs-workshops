/** Lab 2.4: after a navigation, move focus to the new page's heading and announce it. Renders nothing yet. */
// TODO(lab-2.4): take `mainRef`; on pathname change ONLY (not search params) focus main's <h1> if it has a tabindex, else main; set document.title; write the title into an EMPTY aria-live="polite" region; a ref remembers the last announced path
export function RouteAnnouncer() {
  return null;
}
