/** Lab 2.1: a single-key shortcut on the window for the component's lifetime. Does nothing yet. */
// TODO(lab-2.1): a keydown listener in an effect; skip modifier combos and typing targets (input/textarea/select/contentEditable); preventDefault; call the handler through useLatest so the listener is added once
export function useKeyboardShortcut(_key: string, _handler: (event: KeyboardEvent) => void): void {}
