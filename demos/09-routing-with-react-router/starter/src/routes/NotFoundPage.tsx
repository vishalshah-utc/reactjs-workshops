/** Lab 2.2: say which URL wasn't found, and offer a way back. */
// TODO(lab-2.2): useLocation() for the pathname; a <Button as={Link} to="/products">
export function NotFoundPage() {
  return <p>Not found.</p>;
}
