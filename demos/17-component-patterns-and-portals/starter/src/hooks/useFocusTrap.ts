import type { RefObject } from 'react';

// TODO(lab-4.2): while `active`: remember document.activeElement, move focus into the container, wrap Tab / Shift+Tab
// at the ends, and on cleanup give focus BACK to the opener
/** Placeholder: does nothing. */
export function useFocusTrap(_containerRef: RefObject<HTMLElement | null>, _active: boolean): void {}
