/**
 * TODO(lab-5.6): the last eight products opened from the console, surviving a
 * reload.
 *
 * The list is persisted; the TITLES are not. A title is the catalogue's, and
 * caching somebody else's data in localStorage is how a UI shows a name that
 * was changed three weeks ago. So the store keeps ids, `selectRecentProducts`
 * pairs them with a title when the product happens to be on the page, and the
 * badge falls back to `#id`.
 */
export function RecentlyInspected() {
  return null;
}
